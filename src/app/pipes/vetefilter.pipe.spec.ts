import { VetefilterPipe } from './vetefilter.pipe';

describe('VetefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VetefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
