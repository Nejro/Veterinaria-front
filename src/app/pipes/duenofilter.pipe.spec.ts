import { DuenofilterPipe } from './duenofilter.pipe';

describe('DuenofilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DuenofilterPipe();
    expect(pipe).toBeTruthy();
  });
});
